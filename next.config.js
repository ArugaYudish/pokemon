/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['raw.githubusercontent.com'], // Tambahkan domain dari URL gambar eksternal yang diperbolehkan
    },
}

module.exports = nextConfig
